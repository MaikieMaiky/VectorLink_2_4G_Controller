# 参考工程协议备注

参考工程仅给出了两条草案信息：

- NRF24L01 数据包上限按 32 bytes 规划。
- 硬件报告草案以 `0x00` 开头，随后包含 LH、LV、RH、RV 和 KEY。

这些内容不是当前项目的正式协议，不能直接作为线上的稳定格式。正式协议设计时至少需要明确：

- 协议版本和消息类型。
- 四路摇杆的数据宽度、符号、范围和字节序。
- 12 个按键的位定义。
- 包序号、配对标识和失联处理。
- 是否使用 NRF 硬件 CRC 之外的应用层校验。
- 回传遥测字段和向后兼容策略。

在完成 NRF 基础收发测试前，不冻结具体字节布局。
